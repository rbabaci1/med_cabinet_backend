const users = require("./users");
const auth = require("./auth");
const products = require("./products");
const dispensaries = require("./dispensaries");

module.exports = { users, auth, products, dispensaries };
