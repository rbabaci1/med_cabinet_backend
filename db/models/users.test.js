const db = require("../dbConfig");
const User = require("./users");

describe("users.js db models", () => {
  describe("getProducts()", () => {
    it("returns all the products of a specified user", async () => {});
  });
});
